"""Plugin package: ${PLUGIN_NAME}.

This plugin is loaded by DataLens via `manifest.json` and `plugin.py`.

Subpackages are generated depending on the plugin kind:

- `ui/`: widgets/panels for workspace UI (WORKSPACE plugins)
- `services/`: non-UI logic/use-cases (SERVICE/DATASOURCE/MODEL plugins)
"""

