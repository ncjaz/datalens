"""
UI diagnostics utilities (opt-in).

These helpers are intended for developer troubleshooting and are typically
enabled via CLI flags (e.g. `--debug-ui`). They should have zero impact on
normal runtime behavior.
"""

