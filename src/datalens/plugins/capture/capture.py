"""Capture plugin entrypoints (placeholder for now).

TODO(v2): Replace this placeholder workspace with a real capture UI/pipeline.
High-rate capture should not use the generic `IoWriter`; it should use a dedicated
recording pipeline (per the persistence plan).
"""


class CaptureWorkspace:
    """Placeholder workspace entrypoint for the capture plugin."""
