Services
========

V2 services expose the "application use-cases" that the UI and plugins call into
(project loading, persistence, background discovery, etc.).

This section mirrors the package layout so navigation stays readable as the
codebase grows.

.. toctree::
   :maxdepth: 2

   services/index
